name: CI
on:
  push:
    branches:
      - v2/develop
jobs:
  split-upm:
    name: split upm branch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: split upm branch
        run: |
          git subtree split -P "$PKG_ROOT" -b v2/main
          git push -u origin v2/main
        env:
          PKG_ROOT: Assets/Guyl.AtomsVS
